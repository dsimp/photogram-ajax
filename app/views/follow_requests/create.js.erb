$(".<%= dom_id(@follow_request.sender) %>_<%= dom_id(@follow_request.recipient) %>").replaceWith("<%=j(render 'follow_requests/follow_unfollow', sender: @follow_request.sender, recipient: @follow_request.recipient)%>");

console.log("created follow")
